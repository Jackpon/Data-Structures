//#include "SqStack.h"
#include "LinkStack.h"

int main()
{
	//int i;
	//ElemType result;
	////顺序存储结构实现实验
	//SqStack my_stack1;
	//InitStack(my_stack1);
	//for (i = 1; i <= 10; i++)
	//	Push(my_stack1, i + 10);//数据压栈，断点①
	//TraverseStack(my_stack1);
	//while (StackEmpty(my_stack1) != 1)
	//	Pop(my_stack1, result);//数据出栈，断点②


	//SNode  *my_stack2;//链式存储结构实现实验
	//InitStack(my_stack2);
	//for (i = 1; i <= 10; i++)
	//	Push(my_stack2, i + 10);//数据压栈，断点③
	//TraverseStack(my_stack2);
	//while (StackEmpty(my_stack2) != 1)
	//	Pop(my_stack2, result);//数据出栈，断点④

	//Destroy(my_stack2);
	char a[] = "abcdba";
	cout << IsReverse(a);
	
	/*Convert(4,2);*/
	return 1;
}